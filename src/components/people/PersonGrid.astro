---
import PersonCard from "./PersonCard.astro";

interface Props {
  people: Array<{
    name: string;
    slug: string;
    role: string;
    photo?: { src: string; alt: string };
    title?: string;
    bio?: string;
    ccaConnection?: string;
  }>;
  heading?: string;
}

const { people, heading } = Astro.props;
---

<div class:list={["person-grid-section", { "has-heading": !!heading }]}>
  {heading && <h2 class="section-heading">{heading}</h2>}
  <div class="grid-cards">
    {people.map((person) => (
      <PersonCard person={person} />
    ))}
  </div>
</div>

<style>
  .person-grid-section.has-heading {
    padding-block: var(--space-8);
  }

  .person-grid-section:not(.has-heading) {
    padding-top: var(--space-6);
  }

  .section-heading {
    font-size: var(--text-2xl);
    margin-bottom: var(--space-6);
  }
</style>
