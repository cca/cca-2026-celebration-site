---
/**
 * BentoEventCard — Size-aware event card for bento grid layout.
 * Supports large (2×2), tall (1×2), wide (2×1), and compact (1×1) variants.
 */
import type { CollectionEntry } from "astro:content";
import { formatDate, formatDateRange } from "../../lib/utils/format";

interface Props {
  event: CollectionEntry<"events">;
  size: "large" | "tall" | "wide" | "compact";
  class?: string;
}

const { event, size, class: className } = Astro.props;
const { title, shortTitle, slug, date, endDate, location, description, type, degreeLevel, themeKey } = event.data;

function getEventUrl(type: string, degreeLevel: string, slug: string): string {
  if (type === "celebration") return `/commencement/${slug}/`;
  const typeMap: Record<string, string> = {
    commencement: "/commencement/",
    showcase: "/thesis/showcase/",
    "thesis-exhibition": "/thesis/",
  };
  const base = typeMap[type] ?? "/";
  if (degreeLevel === "all") return base;
  return `${base}${degreeLevel}/`;
}

const href = getEventUrl(type, degreeLevel, slug);
const dateDisplay = endDate ? formatDateRange(date, endDate) : formatDate(date);

const badgeLabels: Record<string, string> = {
  commencement: "COMMENCEMENT",
  showcase: "SHOWCASE",
  "thesis-exhibition": "THESIS",
  celebration: "CELEBRATION",
};
const badgeLabel = badgeLabels[type] ?? type.toUpperCase();
---

<a href={href} class:list={['bento-card', `bento-card--${size}`, 'reveal', className]} data-theme={size === 'large' ? themeKey : undefined}>
  <article>
    <span class="bento-badge">{badgeLabel}</span>

    {size === 'compact' ? (
      <>
        <h3 class="bento-title">{shortTitle || title}</h3>
        <time class="bento-date">{dateDisplay}</time>
      </>
    ) : size === 'wide' ? (
      <>
        <h3 class="bento-title">{title}</h3>
        <div class="bento-meta">
          <time class="bento-date">{dateDisplay}</time>
          <p class="bento-location">{location}</p>
        </div>
      </>
    ) : (
      <>
        <h3 class="bento-title">{title}</h3>
        <time class="bento-date">{dateDisplay}</time>
        <p class="bento-location">{location}</p>
        {description && <p class="bento-desc">{description}</p>}
      </>
    )}
  </article>
</a>

<style>
  /* ---- Base card ---- */
  .bento-card {
    display: flex;
    text-decoration: none;
    color: var(--color-text);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-left: none;
    padding: var(--space-6);
    padding-left: calc(var(--space-6) + 3px);
    border-radius: var(--radius-card);
    box-shadow: var(--shadow-card-rest);
    position: relative;
    transition: transform var(--duration-base) var(--ease-out-expo),
                box-shadow var(--duration-base) var(--ease-out-expo);
  }

  /* Prismatic left border */
  .bento-card::before {
    content: "";
    position: absolute;
    top: 30%;
    left: 0;
    height: 40%;
    width: 3px;
    background: var(--gradient-prismatic);
    border-radius: 4px 0 0 4px;
    transition: top var(--duration-base) var(--ease-out-expo),
                height var(--duration-base) var(--ease-out-expo);
  }

  .bento-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-card-hover);
  }

  .bento-card:hover::before {
    top: 0;
    height: 100%;
  }

  @media (prefers-reduced-motion: no-preference) {
    .bento-card::before {
      animation: prismatic-shift 6s linear infinite;
    }

    .bento-card:hover::before {
      animation-duration: 2s;
    }
  }

  /* ---- Large variant — full gradient background ---- */
  .bento-card--large {
    background: var(--theme-gradient);
    color: #fff;
    border: none;
    padding: var(--space-8);
    padding-left: var(--space-8);
    flex-direction: column;
    justify-content: flex-end;
  }

  .bento-card--large::before {
    display: none;
  }

  .bento-card--large .bento-badge {
    background: rgba(255, 255, 255, 0.2);
    color: #fff;
  }

  .bento-card--large .bento-date {
    color: rgba(255, 255, 255, 0.9);
    background: rgba(255, 255, 255, 0.1);
  }

  .bento-card--large .bento-location,
  .bento-card--large .bento-desc {
    color: rgba(255, 255, 255, 0.75);
  }

  .bento-card--large .bento-title {
    font-size: var(--text-2xl);
  }

  /* ---- Tall variant ---- */
  .bento-card--tall {
    flex-direction: column;
  }

  /* ---- Wide variant — horizontal layout ---- */
  .bento-card--wide {
    align-items: center;
    gap: var(--space-4);
  }

  .bento-card--wide article {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: var(--space-2) var(--space-4);
  }

  .bento-card--wide .bento-badge {
    flex-shrink: 0;
  }

  .bento-card--wide .bento-title {
    flex: 1 1 auto;
    margin-bottom: 0;
  }

  .bento-card--wide .bento-meta {
    width: 100%;
    display: flex;
    gap: var(--space-3);
    align-items: center;
  }

  /* ---- Compact variant ---- */
  .bento-card--compact {
    padding: var(--space-4);
    padding-left: calc(var(--space-4) + 3px);
  }

  .bento-card--compact .bento-title {
    font-size: var(--text-lg);
    margin-bottom: var(--space-1);
  }

  /* ---- Shared inner elements ---- */
  .bento-badge {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    background: var(--color-accent-tint);
    color: var(--color-accent);
    padding: var(--space-1) var(--space-2);
    border-radius: 3px;
    margin-bottom: var(--space-3);
  }

  .bento-title {
    font-size: var(--text-xl);
    margin-bottom: var(--space-2);
  }

  .bento-date {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    color: var(--color-accent);
    background: var(--color-surface);
    padding: var(--space-1) var(--space-2);
    border-radius: 3px;
    margin-bottom: var(--space-1);
  }

  .bento-location {
    font-size: var(--text-sm);
    color: var(--color-muted);
    margin-bottom: var(--space-3);
  }

  .bento-desc {
    font-size: var(--text-sm);
    color: var(--color-muted);
  }
</style>
